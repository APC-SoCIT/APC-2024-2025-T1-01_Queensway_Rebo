{
  "name": "Laravel Dev with MariaDB",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "forwardPorts": [8000, 3306],
  "postCreateCommand": "cp .env.example .env && composer install && php artisan key:generate",
  "customizations": {
    "vscode": {
      "extensions": [
        "xdebug.php-debug",
        "bmewburn.vscode-intelephense-client"
      ]
    }
  },
  "remoteUser": "www-data"
}
